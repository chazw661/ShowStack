---
phase: 10-autocomplete-png-export-new-shapes
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - planner/static/planner/js/signal_flow_editor.js
autonomous: true
requirements:
  - LBL-01
  - LBL-02
  - LBL-03
  - EXP-01

must_haves:
  truths:
    - "Typing 1+ characters in #sfd-circuit-label shows a dropdown within 200ms (D-01)"
    - "Dropdown shows label text and source tag per D-02 (e.g. 'FOH Lead — Device Input')"
    - "At most 8 results shown, alphabetical (D-03)"
    - "Arrow keys and Enter select a row; Escape closes without changing input value (D-04)"
    - "Selecting a row sets input.value and triggers scheduleAutosave (D-14)"
    - "Clicking Export PNG button downloads a white-background PNG named per D-06 slug pattern"
    - "A generating toast appears during export; an error toast appears on failure (A3 risk)"
    - "Free text in #sfd-circuit-label still works — autocomplete never blocks free entry (LBL-03)"
  artifacts:
    - path: "planner/static/planner/js/signal_flow_editor.js"
      provides: "Autocomplete combobox widget attached to #sfd-circuit-label; PNG export handler on #sfd-export-png"
      contains: "sfd-label-suggestions"
  key_links:
    - from: "signal_flow_editor.js autocomplete widget"
      to: "signal_flow_label_autocomplete endpoint"
      via: "container.dataset.labelAutocompleteUrl (data-label-autocomplete-url from 10-02)"
      pattern: "labelAutocompleteUrl"
    - from: "#sfd-circuit-label input event"
      to: "scheduleAutosave"
      via: "synthetic input event dispatch after autocomplete selection (D-14)"
      pattern: "dispatchEvent.*input"
    - from: "#sfd-export-png click"
      to: "htmlToImage.toPng(paperEl, ...)"
      via: "D-07 pixelRatio:2, D-09 backgroundColor:'#ffffff'"
      pattern: "htmlToImage.toPng"
---

<objective>
Wire the two JS behaviors introduced as scaffolding in Plans 10-01 and 10-02:

1. Autocomplete combobox — attached to the existing #sfd-circuit-label input in the connector
   inspector. Debounced 200ms fetch to signal_flow_label_autocomplete, ARIA-accessible listbox,
   keyboard navigation (Arrow/Enter/Escape), mouse click, blur-close with 150ms delay, synthetic
   input event dispatch on selection so scheduleAutosave fires without changes to existing code.

2. PNG export handler — click on #sfd-export-png calls htmlToImage.toPng(paperEl, {pixelRatio:2,
   backgroundColor:'#ffffff', width: paper.options.width, height: paper.options.height}), shows a
   generating toast, then triggers a transient <a download> click with the D-06 filename slug.

Purpose: Wave 2 — depends on the endpoint (10-01) and the data attribute + CSS skeleton (10-02).
All code is added inside the existing IIFE in signal_flow_editor.js; no new files created.
Output: signal_flow_editor.js (2 new JS sections appended within the IIFE).
</objective>

<execution_context>
@/Users/charlielawsonmacair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/charlielawsonmacair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-autocomplete-png-export-new-shapes/10-CONTEXT.md
@.planning/phases/10-autocomplete-png-export-new-shapes/10-RESEARCH.md
@.planning/phases/10-autocomplete-png-export-new-shapes/10-01-SUMMARY.md
@.planning/phases/10-autocomplete-png-export-new-shapes/10-02-SUMMARY.md

From signal_flow_editor.js (read before editing to find the exact insertion point):

Key variables already in closure scope (do not redeclare):
  container      — #sfd-container element
  diagramId      — container.dataset.diagramId
  paper          — joint.dia.Paper instance (exposes paper.options.width/height)
  paperEl        — #sfd-paper DOM element
  graph          — joint.dia.Graph instance
  autosaveUrl    — container.dataset.autosaveUrl
  autocompleteUrl — container.dataset.autocompleteUrl
  showToast(msg, level) — toast helper (level: 'info'|'error'|'success')
  getJSON(url)   — fetch GET returning Promise
  scheduleAutosave() — debounced autosave trigger (Phase 9)

New variables to read at IIFE top (after existing reads at lines 26-30):
  var labelAutocompleteUrl = container.dataset.labelAutocompleteUrl;
  // exportPngUrl is not needed — export is fully client-side (html-to-image)

From editor.html (set by 10-02):
  data-label-autocomplete-url on #sfd-container
  #sfd-circuit-label — existing input in inspector (no DOM changes needed; only JS attached)
  #sfd-label-suggestions — <ul role="listbox"> (added by 10-02 CSS but the HTML element
    must be injected by JS or must be present in editor.html)

IMPORTANT: Review editor.html to check whether #sfd-label-suggestions is present as static HTML
or needs to be created by JS. The CSS Section 12 (10-02) styles it but does not create it.
If it is absent from the template, the autocomplete widget must createElement('ul') and append it.
The safest approach: always create it via JS (insertAdjacentElement after #sfd-circuit-label's
parent .sfd-field div). This avoids a template dependency.

From editor.html inspector (lines 120-124):
  <div class="sfd-field">
    <label for="sfd-circuit-label">Circuit label</label>
    <input type="text" id="sfd-circuit-label" placeholder="e.g. CKT-01" maxlength="100">
    <p class="sfd-field-help">Renders along the connector line.</p>
  </div>

D-14 compliance: the existing inspector input listener (from Phase 9) fires on the 'input' event
to call scheduleAutosave. On autocomplete row selection, dispatch a synthetic input event:
  circuitLabelInput.dispatchEvent(new Event('input', { bubbles: true }));
This triggers the existing listener without any changes to Phase 9 code.

html-to-image global: `htmlToImage` (UMD global from vendor/html-to-image.min.js).
Verify it is defined before using: `typeof htmlToImage !== 'undefined'`.

Filename slug logic (D-06):
  var diagramName = container.dataset.diagramName || '';
  var slug = diagramName.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
  var date = new Date().toISOString().slice(0, 10).replace(/-/g, '');
  var filename = (slug || 'signal-flow') + '-' + date + '.png';

Inspector show/hide: the inspector aside (#sfd-inspector) is shown only when a connector is
selected. The autocomplete widget is attached unconditionally at IIFE init time; it only fires
when the user types in #sfd-circuit-label, which is only reachable when the inspector is visible.
No guard needed around event attachment.

Blur close timing: use a 150ms delay on blur so a row click registers before the listbox closes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement autocomplete combobox for #sfd-circuit-label</name>
  <files>planner/static/planner/js/signal_flow_editor.js</files>
  <action>
Read the end of signal_flow_editor.js (last 80 lines) to find the correct insertion point inside
the IIFE (before the closing })(); ). Also read lines 26-31 to find where the existing dataset
reads are, and add labelAutocompleteUrl there:

  var labelAutocompleteUrl = container.dataset.labelAutocompleteUrl;

Then find the end of the IIFE body and add the autocomplete section. Use a comment header
matching the existing style:

  // ══════════════════════════════════════════════════════════════
  // Plan 10-03 — Circuit-label autocomplete combobox (LBL-01..03)
  //   Attached to #sfd-circuit-label in the connector inspector.
  //   D-01: 1-char threshold, 200ms debounce.
  //   D-02: shows "label — source tag" format.
  //   D-03: max 8 results, alphabetical (enforced server-side).
  //   D-14: synthetic input event on selection triggers scheduleAutosave.
  // ══════════════════════════════════════════════════════════════

Implementation:

  var circuitLabelInput = document.getElementById('sfd-circuit-label');

  // Create the listbox and wrap the input field in a relative-position container.
  // The CSS Section 12 (plan 10-02) styles .sfd-autocomplete-wrapper and
  // #sfd-label-suggestions — we only need the DOM structure here.
  var acListbox = null;
  var acTimer = null;
  var acActiveIndex = -1;

  // DOM-readiness note (plan-checker finding, 2026-05-22):
  // initAutocomplete() is called at the bottom of this IIFE. Safe because
  // editor.html:139 loads signal_flow_editor.js with the `defer` attribute,
  // so DOM is fully parsed before this script evaluates. If the `defer`
  // attribute is ever removed from the <script> tag, wrap the init call in
  // a DOMContentLoaded listener or this will hit null elements.
  function initAutocomplete() {
    if (!circuitLabelInput || !labelAutocompleteUrl) return;

    // Wrap input's parent .sfd-field div — add wrapper class for CSS positioning.
    var fieldDiv = circuitLabelInput.closest('.sfd-field');
    if (!fieldDiv) return;
    fieldDiv.classList.add('sfd-autocomplete-wrapper');

    // Create listbox element.
    acListbox = document.createElement('ul');
    acListbox.id = 'sfd-label-suggestions';
    acListbox.setAttribute('role', 'listbox');
    acListbox.setAttribute('hidden', '');
    fieldDiv.appendChild(acListbox);

    // ARIA: wire combobox role onto the input.
    circuitLabelInput.setAttribute('role', 'combobox');
    circuitLabelInput.setAttribute('aria-autocomplete', 'list');
    circuitLabelInput.setAttribute('aria-expanded', 'false');
    circuitLabelInput.setAttribute('aria-haspopup', 'listbox');
    circuitLabelInput.setAttribute('aria-controls', 'sfd-label-suggestions');

    // Input event — debounced fetch.
    circuitLabelInput.addEventListener('input', function () {
      var q = circuitLabelInput.value.trim();
      if (acTimer) clearTimeout(acTimer);
      if (q.length < 1) { closeAcListbox(); return; }
      acTimer = setTimeout(function () { fetchAcResults(q); }, 200);
    });

    // Keyboard navigation.
    circuitLabelInput.addEventListener('keydown', function (evt) {
      if (!acListbox || acListbox.hasAttribute('hidden')) return;
      var rows = Array.from(acListbox.querySelectorAll('.sfd-ac-row'));
      if (!rows.length) return;
      if (evt.key === 'ArrowDown') {
        evt.preventDefault();
        acActiveIndex = Math.min(acActiveIndex + 1, rows.length - 1);
        updateAcActive(rows);
      } else if (evt.key === 'ArrowUp') {
        evt.preventDefault();
        acActiveIndex = Math.max(acActiveIndex - 1, 0);
        updateAcActive(rows);
      } else if (evt.key === 'Enter') {
        evt.preventDefault();
        if (acActiveIndex >= 0 && rows[acActiveIndex]) {
          selectAcRow(rows[acActiveIndex]);
        }
      } else if (evt.key === 'Escape') {
        evt.preventDefault();
        closeAcListbox();
      }
    });

    // Blur close with delay so click on a row registers first.
    circuitLabelInput.addEventListener('blur', function () {
      setTimeout(closeAcListbox, 150);
    });
  }

  function fetchAcResults(q) {
    if (!labelAutocompleteUrl) return;
    var url = labelAutocompleteUrl + '?q=' + encodeURIComponent(q);
    getJSON(url)
      .then(function (data) { renderAcResults(data.results || []); })
      .catch(function () { closeAcListbox(); });
  }

  function renderAcResults(results) {
    if (!acListbox) return;
    acListbox.innerHTML = '';
    acActiveIndex = -1;
    if (!results.length) { closeAcListbox(); return; }

    results.forEach(function (rec, i) {
      var li = document.createElement('li');
      li.className = 'sfd-ac-row';
      li.setAttribute('role', 'option');
      li.setAttribute('aria-selected', 'false');
      li.id = 'sfd-ac-row-' + i;

      // XSS-safe: textContent only (PATTERNS.md rule).
      var labelSpan = document.createElement('span');
      labelSpan.textContent = rec.label || '';
      var sourceSpan = document.createElement('span');
      sourceSpan.className = 'sfd-ac-source';
      sourceSpan.textContent = rec.source ? ('— ' + rec.source) : '';

      li.appendChild(labelSpan);
      li.appendChild(sourceSpan);

      li.addEventListener('mousedown', function (evt) {
        // mousedown fires before blur — prevent blur from firing first.
        evt.preventDefault();
      });
      li.addEventListener('click', function () { selectAcRow(li, rec.label); });
      li.addEventListener('mouseover', function () {
        var rows = Array.from(acListbox.querySelectorAll('.sfd-ac-row'));
        acActiveIndex = rows.indexOf(li);
        updateAcActive(rows);
      });

      acListbox.appendChild(li);
    });

    acListbox.removeAttribute('hidden');
    circuitLabelInput.setAttribute('aria-expanded', 'true');
  }

  function selectAcRow(rowEl, label) {
    // label may come from the rec or from the row's first span text.
    var chosen = label || (rowEl.querySelector('span') && rowEl.querySelector('span').textContent) || '';
    circuitLabelInput.value = chosen;
    closeAcListbox();
    // D-14: dispatch synthetic input event so existing Phase 9 inspector listener fires.
    circuitLabelInput.dispatchEvent(new Event('input', { bubbles: true }));
  }

  function updateAcActive(rows) {
    rows.forEach(function (r, i) {
      r.setAttribute('aria-selected', String(i === acActiveIndex));
    });
    if (acActiveIndex >= 0) {
      circuitLabelInput.setAttribute('aria-activedescendant', 'sfd-ac-row-' + acActiveIndex);
    }
  }

  function closeAcListbox() {
    if (!acListbox) return;
    acListbox.setAttribute('hidden', '');
    acListbox.innerHTML = '';
    acActiveIndex = -1;
    circuitLabelInput.setAttribute('aria-expanded', 'false');
    circuitLabelInput.removeAttribute('aria-activedescendant');
  }

  initAutocomplete();

Commit: `feat(10-03): implement circuit-label autocomplete combobox (LBL-01..03)`
  </action>
  <verify>
    <automated>cd /Users/charlielawsonmacair/DjangoProjects/audiopatch && node -e "
var f=require('fs').readFileSync('planner/static/planner/js/signal_flow_editor.js','utf8');
var checks=[
  ['initAutocomplete', f.includes('initAutocomplete')],
  ['fetchAcResults', f.includes('fetchAcResults')],
  ['sfd-label-suggestions', f.includes('sfd-label-suggestions')],
  ['aria-expanded', f.includes('aria-expanded')],
  ['scheduleAutosave via synthetic input', f.includes('dispatchEvent')],
  ['200ms debounce', f.includes('}, 200)')],
  ['labelAutocompleteUrl', f.includes('labelAutocompleteUrl')],
];
var ok=checks.every(function(c){return c[1];});
checks.forEach(function(c){console.log((c[1]?'PASS':'FAIL')+': '+c[0]);});
process.exit(ok?0:1);
"</automated>
  </verify>
  <done>All 7 checks pass. No syntax errors in editor.js (node parse check passes).</done>
</task>

<task type="auto">
  <name>Task 2: Implement PNG export handler on #sfd-export-png</name>
  <files>planner/static/planner/js/signal_flow_editor.js</files>
  <action>
Read the end of signal_flow_editor.js to confirm the current last line before })(); and find the
insertion point after the autocomplete section added in Task 1.

Add the export section with a comment header:

  // ══════════════════════════════════════════════════════════════
  // Plan 10-03 — PNG export handler (EXP-01)
  //   D-06: filename slug from diagram name + YYYYMMDD date.
  //   D-07: pixelRatio 2 (retina quality).
  //   D-08: captures full paperEl including orphan ghosts.
  //   D-09: white background.
  //   A3: shows generating toast; error toast on failure.
  //   A5: guards typeof htmlToImage before calling.
  // ══════════════════════════════════════════════════════════════

  var exportPngBtn = document.getElementById('sfd-export-png');

  if (exportPngBtn) {
    exportPngBtn.addEventListener('click', function () {
      // A5: guard against missing UMD global (mis-loaded vendor bundle).
      if (typeof htmlToImage === 'undefined') {
        showToast('Export unavailable: html-to-image library not loaded.', 'error');
        return;
      }

      // Disable button during export to prevent double-clicks.
      exportPngBtn.disabled = true;
      showToast('Generating PNG…', 'info');

      // D-06: filename slug.
      var diagramName = (container.dataset.diagramName || '').trim();
      var slug = diagramName.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
      var date = new Date().toISOString().slice(0, 10).replace(/-/g, '');
      var filename = (slug || 'signal-flow') + '-' + date + '.png';

      // D-07, D-08, D-09: full canvas, 2x, white background.
      htmlToImage.toPng(paperEl, {
        pixelRatio: 2,
        backgroundColor: '#ffffff',
        width: paper.options.width,
        height: paper.options.height,
      })
        .then(function (dataUrl) {
          var a = document.createElement('a');
          a.download = filename;
          a.href = dataUrl;
          a.click();
          showToast('PNG exported.', 'success');
        })
        .catch(function (err) {
          console.error('[SFD] PNG export failed', err);
          showToast('Export failed. Try again.', 'error');
        })
        .finally(function () {
          exportPngBtn.disabled = false;
        });
    });
  }

Note: .finally() is supported in all modern browsers and Node 10+. If the project's browser
targets require IE11 compat, replace with a .then().catch() chain that re-enables the button in
both branches. For this codebase (ShowStack is a staff-only tool, modern browsers only), .finally
is acceptable.

Commit: `feat(10-03): implement PNG export handler (EXP-01)`
  </action>
  <verify>
    <automated>cd /Users/charlielawsonmacair/DjangoProjects/audiopatch && node -e "
var f=require('fs').readFileSync('planner/static/planner/js/signal_flow_editor.js','utf8');
var checks=[
  ['htmlToImage.toPng', f.includes('htmlToImage.toPng')],
  ['pixelRatio: 2', f.includes('pixelRatio: 2')],
  ['backgroundColor', f.includes(\"backgroundColor: '#ffffff'\")],
  ['paper.options.width', f.includes('paper.options.width')],
  ['a.download', f.includes('a.download')],
  ['exportPngBtn disabled guard', f.includes('exportPngBtn.disabled')],
  ['slug logic', f.includes('replace(/[^a-z0-9]+')],
];
var ok=checks.every(function(c){return c[1];});
checks.forEach(function(c){console.log((c[1]?'PASS':'FAIL')+': '+c[0]);});
process.exit(ok?0:1);
"</automated>
  </verify>
  <done>All 7 checks pass. Export handler present with correct options. Node parse check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Autocomplete fetch | GET to label-autocomplete endpoint — IDOR guard enforced server-side (10-01); client renders response |
| PNG export | Client-side only; renders DOM to canvas; no server round-trip; no data leaves the browser except the download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | XSS | Autocomplete row render — label and source from server JSON | mitigate | All row content via textContent only (not innerHTML); matches PATTERNS.md XSS rule already established in renderPickerResults |
| T-10-09 | Information Disclosure | PNG export captures full canvas | accept | Export is user-initiated; engineer is intentionally sharing the diagram; white-background full-canvas is the expected behavior (D-08) |
| T-10-10 | Denial of Service | html-to-image toPng memory usage at pixelRatio:2 for 4000x3000 canvas | accept | Generates an 8000x6000 px bitmap (~144MB RGBA uncompressed); PNG compression reduces to ~5-15MB; button disabled during generation prevents stacking; engineers use modern hardware; acceptable risk per A3 |
| T-10-11 | Elevation of Privilege | Autocomplete widget reads labelAutocompleteUrl from DOM | accept | URL comes from Django template (trusted server-rendered value); not user-controllable; same pattern as all other data-* URL attributes in the codebase |
</threat_model>

<verification>
After both tasks:

```bash
cd /Users/charlielawsonmacair/DjangoProjects/audiopatch

# JS content checks
node -e "
var f=require('fs').readFileSync('planner/static/planner/js/signal_flow_editor.js','utf8');
var required=[
  'initAutocomplete','fetchAcResults','sfd-label-suggestions','aria-expanded',
  'dispatchEvent','labelAutocompleteUrl',
  'htmlToImage.toPng','pixelRatio: 2',\"backgroundColor: '#ffffff'\",'paper.options.width',
  'a.download','replace(/[^a-z0-9]+'
];
required.forEach(function(t){console.log((f.includes(t)?'PASS':'FAIL')+': '+t);});
"

# Django system check still passes
python manage.py check
```

Manual functional test (requires both Wave 1 plans deployed):
1. Open a diagram with DeviceInput records that have signal_name set
2. Click a connector to open the inspector
3. Click #sfd-circuit-label and type 1+ characters — dropdown should appear below input
4. Arrow keys should move selection; Enter should populate input and close dropdown
5. Escape should close dropdown without changing input value
6. Input value after selection should trigger autosave (watch Network tab for POST to /save/)
7. Click Export PNG — browser should download a .png file within 1-3 seconds
8. Verify PNG filename matches slug pattern (diagram-name-YYYYMMDD.png)
9. Open PNG — white background, full canvas including any orphan ghosts
</verification>

<success_criteria>
- All JS content checks pass
- `python manage.py check` exits 0
- Autocomplete dropdown appears on 1-char input, max 8 results, correct source tags
- Keyboard nav (Arrow/Enter/Escape) works per D-01 spec
- Selecting a suggestion calls scheduleAutosave (verified via Network tab POST)
- PNG download triggers on button click; file is white-background full canvas at 2x
- Export PNG button re-enables after download completes
- LBL-01, LBL-02, LBL-03, EXP-01 all verifiably working end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/10-autocomplete-png-export-new-shapes/10-03-SUMMARY.md`
using the template at `~/.claude/get-shit-done/templates/summary.md`.

Include:
- The autocomplete data flow: input event -> debounce -> fetch labelAutocompleteUrl -> render listbox
- The D-14 wiring: synthetic input event dispatch after row selection
- The PNG export data flow: click -> disable button -> toast -> htmlToImage.toPng -> a.download -> re-enable
- Assumption A3 and A5 outcomes if they differ from expected (test during manual verification)
- Confirmation that LBL-01, LBL-02, LBL-03, EXP-01 are fully closed
</output>
