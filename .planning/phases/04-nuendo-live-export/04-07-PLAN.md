---
phase: 04-nuendo-live-export
plan: 07
type: execute
wave: 3
depends_on: [05]
files_modified:
  - planner/templates/planner/multitrack/editor.html
autonomous: true
requirements: [NLP-01]
requirements_addressed: [NLP-01]

must_haves:
  truths:
    - "The multitrack editor toolbar renders THREE export anchors: Reaper (.RPP), Reaper (Track Template), and Nuendo Live (.nlpr) — in that order."
    - "All three buttons render for any session regardless of `session.target_daw` (D-11) — same posture as Phase 1's Reaper buttons."
    - "The Nuendo Live button uses `mts-btn mts-btn-success` (matching the .RPP primary-success styling, D-13) — no new CSS class introduced."
    - "Clicking the Nuendo Live button issues `GET /audiopatch/multitrack/<session.id>/export.nlpr/` via Django's `{% url %}` tag."
    - "There is exactly ONE Nuendo Live button — no separate 'Nuendo Live track template' variant (D-14)."
  artifacts:
    - path: "planner/templates/planner/multitrack/editor.html"
      provides: "Third toolbar anchor for Nuendo Live download"
      contains: "multitrack_export_nlpr"
  key_links:
    - from: "editor.html toolbar anchor"
      to: "URL name 'multitrack_export_nlpr' (Plan 05)"
      via: "{% url 'planner:multitrack_export_nlpr' session.id %}"
      pattern: "multitrack_export_nlpr"
---

<objective>
Add the third export button to the multitrack editor's toolbar. Engineers
now see three side-by-side download anchors, all enabled for every
session: Reaper (.RPP), Reaper (Track Template), and Nuendo Live (.nlpr).

This plan is the final piece of NLP-01's UX: with the form gate removed
(Plan 06), engineers can create new sessions with `target_daw='nuendo_live'`;
with this button in place, they can click to download the `.nlpr` from
any session (regardless of `target_daw`, per D-11).

Purpose: This is a one-line template insertion — the smallest plan in the
phase, but it's also the most user-visible. The button copy and styling
were locked in CONTEXT.md D-13 (style) and D-11 (always-visible).

Output: One new `<a>` anchor in
`planner/templates/planner/multitrack/editor.html` immediately after the
existing Reaper `(Track Template)` anchor at line 81. Zero other changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nuendo-live-export/04-CONTEXT.md
@.planning/phases/04-nuendo-live-export/04-RESEARCH.md
@.planning/phases/04-nuendo-live-export/04-PATTERNS.md
@planner/templates/planner/multitrack/editor.html

<interfaces>
<!-- planner/templates/planner/multitrack/editor.html:75-82 — existing toolbar -->
```html
<div class="mts-toolbar-actions">
  <button type="button" class="mts-btn mts-btn-secondary" onclick="mtsOpenPicker('inputs')">+ Add tracks</button>
  <a class="mts-btn mts-btn-success"
     href="{% url 'planner:multitrack_export_rpp' session.id %}">Export to Reaper (.RPP)</a>
  <a class="mts-btn mts-btn-secondary"
     href="{% url 'planner:multitrack_export_rtracktemplate' session.id %}"
     title="Reaper track template — import via Track menu › Insert track from template">Export to Reaper (Track Template)</a>
</div>
```

<!-- Plan 05 just registered the URL name 'multitrack_export_nlpr' -->
```
path('multitrack/<int:session_id>/export.nlpr/', views.multitrack_export_nlpr, name='multitrack_export_nlpr')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Nuendo Live toolbar button to editor.html</name>
  <files>planner/templates/planner/multitrack/editor.html</files>
  <read_first>
    - planner/templates/planner/multitrack/editor.html (lines 70-90 — the toolbar-actions div including all three existing children and the closing tag)
    - .planning/phases/04-nuendo-live-export/04-CONTEXT.md D-11 (always-visible)
    - .planning/phases/04-nuendo-live-export/04-CONTEXT.md D-13 (style reuse — `mts-btn mts-btn-success`)
    - .planning/phases/04-nuendo-live-export/04-CONTEXT.md D-14 (single button — no Nuendo track-template variant)
    - .planning/phases/04-nuendo-live-export/04-PATTERNS.md §"planner/templates/.../editor.html — add 3rd toolbar button"
  </read_first>
  <action>
    Insert exactly one new `<a>` element immediately AFTER the existing
    `Reaper (Track Template)` anchor (currently lines 79-81) and BEFORE
    the closing `</div>` of `<div class="mts-toolbar-actions">` (line 82).

    The exact insertion (use 2-space indentation matching the surrounding
    anchors — confirm by reading the existing indentation in the file
    once before editing; it appears to be 6 spaces from the start of the
    line):

    ```html
          <a class="mts-btn mts-btn-success"
             href="{% url 'planner:multitrack_export_nlpr' session.id %}">Export to Nuendo Live (.nlpr)</a>
    ```

    Final toolbar block should read:
    ```html
        <div class="mts-toolbar-actions">
          <button type="button" class="mts-btn mts-btn-secondary" onclick="mtsOpenPicker('inputs')">+ Add tracks</button>
          <a class="mts-btn mts-btn-success"
             href="{% url 'planner:multitrack_export_rpp' session.id %}">Export to Reaper (.RPP)</a>
          <a class="mts-btn mts-btn-secondary"
             href="{% url 'planner:multitrack_export_rtracktemplate' session.id %}"
             title="Reaper track template — import via Track menu › Insert track from template">Export to Reaper (Track Template)</a>
          <a class="mts-btn mts-btn-success"
             href="{% url 'planner:multitrack_export_nlpr' session.id %}">Export to Nuendo Live (.nlpr)</a>
        </div>
    ```

    Notes:
    - Class is `mts-btn mts-btn-success` (matches the `.RPP` primary
      anchor; D-13 locked this). NO new CSS class introduced.
    - Button label is `Export to Nuendo Live (.nlpr)` (parallel to
      `Export to Reaper (.RPP)`).
    - No `title=` attribute on this anchor — the label is unambiguous on
      its own (the Reaper track-template anchor has one because the
      `.RTrackTemplate` extension isn't self-explanatory; `.nlpr` is
      the obvious Nuendo Live file).
    - No `target="_blank"`, no `download` attribute, no `onclick` —
      Django's `Content-Disposition: attachment` (set by Plan 05's view)
      triggers the browser's download flow without explicit hints.
    - No JS. No CSS edit. No other file touched in this plan.

    The button renders for every session regardless of
    `session.target_daw` — there is no `{% if %}` wrapper around it.
    D-11 locked this: all three buttons always show. (CONTEXT.md notes
    that `target_daw`'s semantic meaning shifts to "default for new
    sessions / template signaling" rather than "what buttons appear" —
    don't refactor that field away.)
  </action>
  <verify>
    <automated>cd /Users/charlielawsonmacair/DjangoProjects/audiopatch && python -c "
import django, os
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'audiopatch.settings')
django.setup()
import pathlib
tpl = pathlib.Path('planner/templates/planner/multitrack/editor.html').read_text()

# Count three export anchors total
anchor_count = tpl.count('class=\"mts-btn mts-btn-success\"\\n')
# Also accept single-line form
flat_success = tpl.count('mts-btn mts-btn-success')
assert flat_success >= 2, f'Expected at least 2 mts-btn-success anchors (RPP + nlpr), got {flat_success}'
assert tpl.count('multitrack_export_nlpr') == 1, 'Expected exactly one multitrack_export_nlpr reference'
assert tpl.count('Export to Nuendo Live (.nlpr)') == 1, 'Expected exactly one Nuendo button label'
assert tpl.count('multitrack_export_rpp') == 1, '.RPP button missing or duplicated'
assert tpl.count('multitrack_export_rtracktemplate') == 1, 'RTrackTemplate button missing or duplicated'

# Confirm the new button is INSIDE the toolbar-actions div
toolbar_start = tpl.index('mts-toolbar-actions')
toolbar_end = tpl.index('</div>', toolbar_start)
nuendo_pos = tpl.index('multitrack_export_nlpr')
assert toolbar_start < nuendo_pos < toolbar_end, 'Nuendo button outside toolbar-actions div'

# Render the template against a fake context to confirm {% url %} reverses
from django.template import Context, Template
# Quick check: load the template and confirm Django can parse it
from django.template.loader import get_template
get_template('planner/multitrack/editor.html')

# Confirm reverse() also works (Plan 05's URL must already be registered)
from django.urls import reverse
url = reverse('planner:multitrack_export_nlpr', args=[1])
assert url.endswith('/multitrack/1/export.nlpr/'), f'Unexpected reverse: {url}'

print('OK')
" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "{% url 'planner:multitrack_export_nlpr' session.id %}" planner/templates/planner/multitrack/editor.html` returns `1`.
    - `grep -c 'Export to Nuendo Live (.nlpr)' planner/templates/planner/multitrack/editor.html` returns `1`.
    - `grep -c 'mts-btn mts-btn-success' planner/templates/planner/multitrack/editor.html` returns at least `2` (existing `.RPP` + new `.nlpr`).
    - `grep -c 'multitrack_export_rpp' planner/templates/planner/multitrack/editor.html` returns `1` (unchanged).
    - `grep -c 'multitrack_export_rtracktemplate' planner/templates/planner/multitrack/editor.html` returns `1` (unchanged).
    - `grep -c 'multitrack_export_nlpr' planner/templates/planner/multitrack/editor.html` returns `1` (exactly one new button, no duplicates).
    - The new button is inside the `<div class="mts-toolbar-actions">` block (the verify command's position check enforces this).
    - There is NO `{% if %}` wrapper around any of the three buttons (D-11) — `grep -B2 'multitrack_export_nlpr' planner/templates/planner/multitrack/editor.html` shows no `{% if %}` immediately above the anchor.
    - `python manage.py check` exits 0.
    - Django template loader can parse the file without error (the verify command's `get_template` call enforces this).
    - `reverse('planner:multitrack_export_nlpr', args=[1])` succeeds (Plan 05's URL is in place).
    - `python manage.py test planner.tests.test_nuendo_live_export -v 1` exits 0.
    - `python manage.py makemigrations planner --dry-run` reports `No changes detected`.
  </acceptance_criteria>
  <done>
    The editor toolbar renders three export anchors in the documented order
    (`.RPP`, `.RTrackTemplate`, `.nlpr`), all enabled for every session.
    Clicking the new button triggers a GET against
    `/audiopatch/multitrack/<id>/export.nlpr/` and (with Plan 03's
    fixture in place + Plan 05's view active) the engineer receives a
    valid `.nlpr` download.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template-rendered URL → HTTP request | Django's `{% url %}` reverses against the URL conf and emits a same-origin path; not subject to user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Tampering | XSS via `session.id` interpolation | accept | `session.id` is a server-side int from the ORM, not user input. Django's URL reverser produces a safe path string. |
| T-04-23 | Spoofing / CSRF on the GET endpoint | GET-only export view | accept | The view is GET, idempotent, side-effect-free (no DB writes). CSRF tokens are not required for GET in Django's default middleware. Same posture as Phase 1 download buttons. |
</threat_model>

<verification>
After the template edit:

```bash
# One new button, no duplicates.
grep -c 'multitrack_export_nlpr' planner/templates/planner/multitrack/editor.html  # → 1
grep -c 'Export to Nuendo Live' planner/templates/planner/multitrack/editor.html   # → 1

# Existing buttons intact.
grep -c 'multitrack_export_rpp' planner/templates/planner/multitrack/editor.html              # → 1
grep -c 'multitrack_export_rtracktemplate' planner/templates/planner/multitrack/editor.html   # → 1

# Django happy.
python manage.py check
python manage.py test planner.tests.test_nuendo_live_export -v 1

# Manual smoke (engineer): start runserver, visit a session's editor page,
# click "Export to Nuendo Live (.nlpr)" — should trigger a download.
# This is HUMAN-UAT, not an automated check.
```
</verification>

<success_criteria>
- The multitrack editor's `mts-toolbar-actions` div contains the three
  expected anchors in the order: `.RPP`, `.RTrackTemplate`, `.nlpr`.
- The new anchor uses `mts-btn mts-btn-success` and the `{% url %}` tag
  for `planner:multitrack_export_nlpr`.
- No `{% if %}` wrapper gates the new button on `session.target_daw`.
- Django template-loader parses the file without error.
- `python manage.py check` exits 0.
- No edits outside `planner/templates/planner/multitrack/editor.html`.
- `planner.tests.test_nuendo_live_export` still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-nuendo-live-export/04-07-SUMMARY.md`
per `templates/summary.md`.
</output>
